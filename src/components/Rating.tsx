import style from "../components/rating.module.css";

import { FC } from "react";

interface RatingProps {}

const Rating: FC<RatingProps> = ({}) => {
  return <div>Rating component</div>;
};

export default Rating;
